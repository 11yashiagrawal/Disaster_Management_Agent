# Disaster_Management_Agent

## Team Members

- Yashi Agrawal
- Isha Tomar
- Pratyush Chouksey
- Kavya Katal

## Local Setup

1. Create and activate a Python virtual environment.
2. Install dependencies:
	- `pip install -r requirements.txt`
3. Create `.env` from `.env.example` and set `GROQ_API_KEY`.
4. Run the sample parser:
	- `python src/presentation/cli/run_sample_parse.py`